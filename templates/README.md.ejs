# <%= projectName %>

ExpressStart scaffolded project.

## Quick Start
npm install
cp .env.example .env
npm run dev

## Utils
Prototype extensions are available in `src/utils/extend-prototypes.ts`. Call `extendPrototypes()` in index.ts to enable.

## Warnings
- Prototype extensions use non-enumerable properties for safety.
- Test for conflicts if adding libs.
